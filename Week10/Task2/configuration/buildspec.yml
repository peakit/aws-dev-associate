version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
      python: 3.11
    commands:
      - pip install aws-sam-cli
      - echo Installing dependencies...
      - cd ../
      - mvn clean install
  pre_build:
    commands:
      - echo Running tests...
      - cd ../
      - mvn test
  build:
    commands:
      - echo Building jar...
      - cd ../
      - mvn package
      - echo Packaging SAM template...
      - cd ../lambda
      - sam package --template-file sam-template.yaml --s3-bucket $S3_BUCKET --output-template-file sam-template-packaged.yaml
artifacts:
  files:
    - ../lambda/sam-template-packaged.yaml
    - ../target/*.jar
  discard-paths: yes